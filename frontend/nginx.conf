server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        
        # Esta línea es clave para que React Router funcione
        try_files $uri $uri/ /index.html;
    }
}